name: ping-pi

on:
  workflow_dispatch:

jobs:
  ping:
    runs-on: [self-hosted, Linux, ARM64, pi, aquaponics, deploy]

    steps:
      - name: Show runner info
        run: |
          echo "=== WHOAMI ==="
          whoami
          echo "=== HOSTNAME ==="
          hostname
          echo "=== UNAME ==="
          uname -a
          echo "=== HOME ==="
          echo $HOME
          echo "=== DOCKER ==="
          docker version
          echo "=== DEPLOY DIR ==="
          ls -la /home/actions/deploy/aquaponics
